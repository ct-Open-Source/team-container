apiVersion: v1
kind: Service
metadata:
  name: {{ include "teamcloud-chat.name" . }}-rc
spec:
  ports:
    - protocol: TCP
      name: web
      port: 3000
      targetPort: 3000
  selector:
    app: {{ include "teamcloud-chat.name" . }}-rc
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "teamcloud-chat.name" . }}-mongo
spec:
  ports:
    - protocol: TCP
      name: mongo
      port: 27017
      targetPort: 27017
  selector:
    # app: {{ include "teamcloud-chat.name" . }}-mongo
    role: mongo