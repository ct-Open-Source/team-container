apiVersion: v1
kind: Service
metadata:
  name: {{ include "teamcloud-nextcloud.name" . }}-nc
spec:
  ports:
    - protocol: TCP
      name: web
      port: 80
  selector:
    app: {{ include "teamcloud-nextcloud.name" . }}-nc
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "teamcloud-nextcloud.name" . }}-db
spec:
  ports:
    - protocol: TCP
      name: sql
      port: 3306
  selector:
    app: {{ include "teamcloud-nextcloud.name" . }}-db