---
kind: Deployment
apiVersion: apps/v1
metadata:
  namespace: default
  name: {{ include "teamcloud-nextcloud.name" . }}-db
  labels:
    app: {{ include "teamcloud-nextcloud.name" . }}-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ include "teamcloud-nextcloud.name" . }}-db
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: {{ include "teamcloud-nextcloud.name" . }}-db
      annotations:
        timestamp: "{{ now | unixEpoch }}"
    spec:
      volumes:
       - name: teamcloud-db-vol
         persistentVolumeClaim:
           claimName: {{ include "teamcloud-nextcloud.name" . }}-nc-db
      containers:
        - name: {{ include "teamcloud-nextcloud.name" . }}-db
          image: mariadb
          imagePullPolicy: Always
          volumeMounts:
           - name: teamcloud-db-vol
             mountPath: "/var/lib/mysql"
          ports:
            - containerPort: 3306
              name: mysql
          env:
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "teamcloud-nextcloud.name" . }}-secret
                  key: rootpassword
            - name: MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "teamcloud-nextcloud.name" . }}-secret
                  key: password
            - name: MYSQL_DATABASE
              value: "nextcloud"
            - name: MYSQL_USER
              value: "nextcloud"
            - name: MYSQL_INITDB_SKIP_TZINFO
              value: "1"