app:
  name: cloud
  domain: example.org

# storage limit for nextcloud files and database
storage:
  limits:
    data: 128Gi
    database: 128Gi

# no need to change these values. For internal databas account only
database:
  password: "secret#password"
  root_password: "very123secret#passWord!"
